export default function Conversation() {
  return (
    <div>
      <h1>Conversation</h1>
      <p style={{ color: 'var(--ink-500)' }}>Built in a later phase — route is wired up.</p>
    </div>
  )
}
